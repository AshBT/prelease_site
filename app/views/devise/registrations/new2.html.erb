<div id="request-invite" class="modal" style="display: <%= @user.errors.any? ? 'inline' : 'none';%>">
  <%= simple_form_for resource, :as => resource_name, :url => registration_path(resource_name) , :html => {:class => 'form-horizontal' } do |f| %>
    <div class="modal-header">
      <a class="close" data-dismiss="modal">&#215;</a>
      <h3>Join the Contextualyze Private Beta List</h3>
    </div>
    <div class="modal-body">
      <%= f.error_notification %>
      <%= f.input :email, :placeholder => 'your email here!' %>
      <%= f.input :first_name, :placeholder => 'your first name here!' %>
      <%= f.input :last_name, :placeholder => 'your last name here!' %>
      <%= f.input :twitter_handle, :placeholder => 'your twitter handle here!' %>
    </div>
    <div class='aligned'>
      <div class="modal-footer tba" style="vertical-align:middle">
        <%= f.submit "Request Invitation", :class => "btn btn-primary", :id => 'invitation_button' %>
        <a class="btn tba" data-dismiss="modal" href="#">Close</a>
      </div>
    </div>

  <% end %>
</div>

<style type="text/css">
    .modal-footer {
        margin-top: 0;
    }
    .container {
        width: 100%;
    }
    .btn {
        margin-bottom: 0px;
    }
    a.brand {
        color: white;
        text-align: right;
    }

    .modal {
        z-index: 1200;
        top: 25%;
    }

    input.string.optional {
        width: 100%;
    }

</style>